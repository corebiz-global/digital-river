mutation SaveAppSettings($version: String, $settings: String) {
  saveAppSettings(
    app: "motorolaeudevelopment2.connector-digital-river"
    version: $version
    settings: $settings
  ) @context(provider: "vtex.apps-graphql") {
    message
  }
}
